<tr>
  <td><%= question.id %></td>
  <td><%= question.body %></td>
  <td>
    <span><%= link_to 'show', admin_question_path(question) %></span>
    <span><%= link_to 'edit', edit_admin_question_path(question) %></span>
    <span><%= link_to 'delete', admin_question_path(question),
                                method: :delete,
                                data: { confirm: "Are you sure?" } %>
    </span>
  </td>
</tr>
